{% set name = "VARNA" %}
{% set version = "0.93.2" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

build:
  number: 1

source:
  git_url: https://github.com/Sargueil-CiTCoM/VARNA
  git_depth: 1

requirements:
  build:
    - ant
    - openjdk

  run:
    - openjdk


about:
  home: https://github.com/yannponty/VARNA 
  license: GPL3
  license_family: GPL3
  summary: Visualization Application for RNA  
